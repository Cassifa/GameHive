# Core Dependencies
torch==2.8.0+cpu  # 根据实际安装情况，如果是 GPU 版则为 torch==2.x.x+cu118
numpy==1.26.4     # 推荐版本，避免太高版本在旧环境中可能的问题
onnx==1.18.0
onnxruntime==1.22.0

# PyTorch (GPU Version Recommended)
# Note: Installing PyTorch via pip usually requires specifying the index-url for the correct CUDA version.
# For example, for CUDA 11.8:
# --index-url https://download.pytorch.org/whl/cu118
# torch>=2.0.0

# Python Version (Reference)
# python==3.9.23
